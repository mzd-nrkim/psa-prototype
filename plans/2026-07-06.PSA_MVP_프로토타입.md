# PSA MVP 프로토타입 — 임원 발표용 동작 데모

> 상태: 게이트통과-머지대기

사내 PSA(프로젝트 경제성 레이어)의 핵심 가치인 **"이 프로젝트 남는 장사인가"**를 임원에게 증명하기 위한 **동작 프로토타입**을 만든다. 더존/Salesforce 연동은 범위 밖(확정)이며, 급여 원본도 쓰지 않는다. DB·백엔드 없이 SvelteKit 정적 앱 + JSON 목업 데이터로 3개 화면을 구현해, 5분 데모로 "흩어진 엑셀 → 한 화면, 위험 프로젝트 빨간불 → 원인 드릴다운 → 인력 관점"을 보여준다.

이 프로토타입은 제품이 아니라 **설득 자산**이다. 실제 등급체계·단가·프로젝트 데이터는 미확인이므로, 그럴듯한 플레이스홀더 값을 박아 "자리(컬럼)"를 확정하는 것이 목적이다.

---

## 목표

- 4개 핵심 엔티티(Project·Employee·Grade·Assignment)와 1개 파생 뷰(ProjectPnL)의 **컬럼이 예시 데이터로 채워진 상태**로 확정된다.
- 임원 발표용 **3개 화면**(전사 포트폴리오 / 프로젝트 손익 상세 / 임직원 투입 현황)이 브라우저에서 동작한다.
- 마진(수주 − Σ배정원가)이 **하드코딩이 아니라 목업 데이터로부터 자동 계산**되어, 배정을 바꾸면 마진이 갱신되는 것을 시연할 수 있다.
- 급여 원본 없이 **등급 표준원가율만** 사용하는 원가 산정 구조가 코드로 구현된다.
- DB·백엔드·외부 연동 **없이** 정적 배포 가능한 형태(로컬 `npm run dev` 또는 정적 빌드)로 완성된다.

## 접근 방법

1. **greenfield 스캐폴딩**: `psa_private` 루트에 SvelteKit(정적 어댑터) 프로젝트를 생성한다. lodestar에서 언급된 스택(SvelteKit)을 승계하되, 데모 목적상 Postgres·FastAPI·Docker는 도입하지 않는다. `adapter-static`로 백엔드 없이 배포 가능하게 한다.

2. **데이터 계층 = 단일 JSON 목업 + 파생 계산 모듈**:
   - `src/lib/data/mock.json`(또는 `.ts`)에 Grade·Employee·Project·Assignment 예시 레코드를 담는다.
   - `src/lib/pnl.ts`에 파생 계산 순수 함수를 둔다: `assignmentCost(assignment, grade)` = 등급 표준원가 × M/M, `projectPnl(project, assignments, grades)` = 수주 − Σ배정원가 → {매출, 배정원가합, 마진, 마진율}, `portfolioRollup(projects)` = 전사 합계·마진율.
   - 계산은 전부 이 모듈에 격리해 **단위 테스트 대상**으로 삼는다(손익 로직이 프로토타입의 신뢰 핵심).

3. **3개 라우트 구성**:
   - `/` (전사 포트폴리오): 프로젝트 테이블 + 마진율 신호등(🟢≥30% / 🟡15~30% / 🔴<15%) + 전사 합계 행. 행 클릭 → 상세로 이동.
   - `/project/[code]` (프로젝트 손익 상세): 배정 원장 테이블(직원·등급·M/M·산정원가) + 매출−원가=마진 브레이크다운 + 저마진 경고 문구. 배정 투입률/M/M을 UI에서 조정하면 마진이 실시간 재계산되는 인터랙션 1건 포함(시연 포인트).
   - `/people` (임직원 투입 현황): 직원별 투입률 막대(프로젝트 배정 합산) + 벤치/과투입 표시.

4. **신호등·포맷 유틸 공통화**: 금액(억/만원 축약)·퍼센트·마진 신호등 색상을 `src/lib/format.ts`로 공통화해 3화면이 동일 규칙을 쓴다.

5. **예시 데이터 현실성**: 등급 4단계(초/중/고/특급), 표준원가·청구단가 플레이스홀더, 프로젝트 3~4건(마진 양호 🟢 2건 + 저마진 🔴 1건으로 발표 스토리 성립), 직원 6~8명, 배정으로 마진 숫자가 일관되게 떨어지도록 값 검산. 고객사는 중립 익명(A제조·B금융 등)으로 표기.

6. **발표 보조**: README 또는 `docs/`에 데모 시나리오(화면 순서·클릭 흐름·예상 질문 대응)를 1페이지로 정리(계획 범위에는 화면 구현까지, 시나리오 문서는 선택).

## 실행 시 필수 고려사항

> plan-review 검토 중 발견한 서술형 사항. plan-run 실행 에이전트가 반드시 참고한다.

- **실행 순서·병렬 가능 여부(③)**: greenfield 스캐폴딩이므로 **A(스캐폴딩) → B(데이터 계층) → C(화면)** 순서 의존이 강하다. 화면 3개(C-1/C-2/C-3)는 서로 다른 라우트 파일이라 병렬 가능하나, 반드시 **B의 `types.ts`·`pnl.ts`·`format.ts`·`mock.ts`가 완성된 뒤**에 착수한다(전 화면이 이 공통 lib에 의존). `pnl.ts`(B-3)는 `types.ts`(B-1)에, `mock.ts`(B-2)는 `types.ts`에 의존한다.
- **테스트 하네스·환경 전제(②)**: 이 repo는 **greenfield**로 아직 `package.json`·`node_modules`가 없다. `node_modules`는 gitignore 대상이므로 **워크트리(Z-pre)에서는 `npm install`·`tsc`·`svelte-check`·`vitest`·`build`를 신뢰 실행할 수 없다**. 따라서 타입체크·단위테스트·빌드는 전부 **Node 정적 게이트로 강등** — plan-run 5단계 머지 직후 **원본 main에서 `npm install` 후 실행**한다. e2e(Playwright)는 앱 기동이 필요하므로 **Z-post**(push 후)에 배치한다.
- **회귀 범위(①)**: greenfield 신규 프로젝트라 이번 변경이 깨뜨릴 **기존 테스트·기존 동작이 없다**. 회귀 위험은 repo 내부가 아니라 "목업 데이터의 마진 검산값 불일치"(사람 실수)에 있으므로 `pnl.ts` 단위테스트가 이를 방어한다.
- **디자인 계약 준수**: repo 루트에 이미 `DESIGN.md`(디자인 시스템 계약)가 존재한다. 3개 화면(C) 구현 시 색상·타이포·컴포넌트 규칙은 `DESIGN.md`를 따른다 — 마진 신호등(🟢/🟡/🔴) 색상도 임의값이 아니라 `DESIGN.md` 팔레트에서 파생한다.
- **미선택 결정 근거(④)**: Postgres·FastAPI·Docker는 lodestar 스택에 있으나 **데모 목적상 도입하지 않는다**(백엔드 없이 `adapter-static` 정적 배포가 "DB 없이 배포 가능"을 증명하는 발표 포인트이기 때문). 더존/Salesforce 연동·급여 원본도 범위 밖 확정.
- **신호등 임계값은 결정성 항목**: 마진율 신호등 경계(🟢≥30% / 🟡15~30% / 🔴<15%)는 접근 방법에 명시된 값이다. 목업 데이터는 이 경계에서 🟢2건·🔴1건이 **명확히 갈리도록**(경계 근처 애매값 회피) 검산해 배치한다.

## 작업 목록

### A. 프로젝트 스캐폴딩

- [x] A-1. SvelteKit + adapter-static 스캐폴딩
  - [x] `package.json` 작성 (`svelte`, `@sveltejs/kit`, `@sveltejs/adapter-static`, `vite`, `typescript`, `vitest`, `@playwright/test` devDeps)
  - [x] `svelte.config.js` — `adapter-static` 지정 + prerender 설정
  - [x] `vite.config.ts` — SvelteKit 플러그인 + vitest test 설정
  - [x] `tsconfig.json` / `src/app.d.ts` 기본 타입 설정
  - [x] `src/app.html` + `src/routes/+layout.svelte` 기본 레이아웃 (DESIGN.md 준수)
  - [x] `src/routes/+layout.ts` 에 `export const prerender = true` (정적 빌드 전제)
  - [x] `.gitignore` 확인·보강 (`node_modules`, `/build`, `/.svelte-kit`)

### B. 데이터 계층 (파생 계산 + 목업)

- [x] B-1. 데이터 모델 타입 정의 (`src/lib/types.ts`)
  - [x] `Grade`(등급명·표준원가율·청구단가), `Employee`(id·이름·등급참조), `Project`(code·고객사·수주액·기간), `Assignment`(project·employee·M/M 또는 투입률) 타입
  - [x] 파생 뷰 타입 `ProjectPnL`(매출·배정원가합·마진·마진율) 정의
- [x] B-2. 예시 목업 데이터 작성 (`src/lib/data/mock.ts`)
  - [x] 등급 4단계(초/중/고/특급) 표준원가·청구단가 플레이스홀더
  - [x] 직원 6~8명 (등급 분포)
  - [x] 프로젝트 3~4건 (고객사 중립 익명 A제조·B금융 등) + 배정 세트
  - [x] 마진이 🟢2건·🔴1건으로 명확히 갈리도록 값 검산 (경계 근처 애매값 회피)
- [x] B-3. 파생 계산 모듈 (`src/lib/pnl.ts`, 순수 함수)
  - [x] `assignmentCost(assignment, grade)` = 등급 표준원가 × M/M
  - [x] `projectPnl(project, assignments, grades)` = 수주 − Σ배정원가 → {매출, 배정원가합, 마진, 마진율}
  - [x] `portfolioRollup(projects, ...)` = 전사 합계·마진율
- [x] B-4. 포맷·신호등 유틸 (`src/lib/format.ts`)
  - [x] 금액 축약(억/만원)·퍼센트 포맷 함수
  - [x] 마진 신호등 임계값 함수(🟢≥30% / 🟡15~30% / 🔴<15%) — DESIGN.md 팔레트에서 색상 파생

### C. 화면 (B 완성 후 착수, C-1/C-2/C-3 병렬 가능)

- [x] C-1. 화면① 전사 포트폴리오 (`src/routes/+page.svelte`)
  - [x] 프로젝트 테이블 + 마진율 신호등 컬럼
  - [x] 전사 합계 행 (`portfolioRollup` 사용)
  - [x] 행 클릭 → `/project/[code]` 이동
- [x] C-2. 화면② 프로젝트 손익 상세 (`src/routes/project/[code]/+page.svelte`)
  - [x] 배정 원장 테이블 (직원·등급·M/M·산정원가)
  - [x] 매출−원가=마진 브레이크다운 + 저마진 경고 문구
  - [x] M/M(또는 투입률) 조정 시 마진·마진율 실시간 재계산 인터랙션 (핵심 시연 포인트)
  - [x] 없는 `code` 접근 시 안전 처리(404 또는 안내)
- [x] C-3. 화면③ 임직원 투입 현황 (`src/routes/people/+page.svelte`)
  - [x] 직원별 투입률 막대 (프로젝트 배정 합산)
  - [x] 벤치(미투입)·과투입(>100%) 표시

### D. 테스트·문서

- [x] D-1. `pnl.ts` 단위 테스트 작성 (`src/lib/pnl.test.ts`, vitest)
  - [x] happy-path·경계·역산·교차검증·존재성 케이스 (TC 섹션 참조)
  - (실행·통과 확인은 Node 정적 게이트에서 — Z 섹션)
- [x] D-2. 데모 시나리오 1페이지 작성 (`docs/` — 화면 순서·클릭 흐름·예상 질문 대응) *(선택)*

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

> 스키마 변경 없음 → Z-pre 마이그레이션 항목 없음. Node 프로젝트(greenfield, `node_modules` gitignored) → `npm install`·타입체크·vitest·build는 워크트리에서 신뢰 불가하므로 **Node 정적 게이트로 강등**하여 머지 직후 원본 main에서 실행한다.
> 워크트리 서브에이전트는 **구현 + 테스트 파일 작성 완료 후 반드시 워크트리 브랜치에 커밋**한다. `npm install`·단위테스트 실행·빌드·e2e·main 머지는 **메인의 책임**이다.

#### Z-pre. 머지 전 (워크트리에서 실행)

- [x] 소스 파일 존재·구조 정적 확인 (`src/lib/pnl.ts`·`format.ts`·`types.ts`·`mock.ts`, 3개 라우트 파일, `pnl.test.ts` 존재)
- (이 프로젝트는 `node_modules` 부재로 워크트리에서 실행 가능한 컴파일·테스트 게이트가 없음 → 아래 Node 정적 게이트로 이관)

#### Node 정적 게이트 (머지 직후, 원본 main에서 실행)

- [ ] 원본 main에서 `npm install`
- [ ] `npm run check`(svelte-check/tsc) 통과 — 타입 오류 0
- [ ] `npm run test`(vitest) 통과 — `pnl.ts` 단위 테스트 전부 green (통과/실패 카운트 근거 동반)
- [ ] `npm run build`(adapter-static) 에러 없이 정적 산출물 생성 → 백엔드/DB 없이 배포 가능 증명

#### Z-post. push 후 (앱 기동 환경에서 실행)

- [ ] e2e 통합테스트 통과 확인 (`npx playwright test`, `npm run dev`/preview 기동 전제)
  - [ ] `e2e/psa-demo.spec.ts` 신규 작성 (미존재 시 필수)
    - 3개 라우트(`/`, `/project/[code]`, `/people`) 렌더링 확인
    - 화면① 신호등 색상 구분(🟢/🔴) + 전사 합계 행 표시
    - 화면② M/M 조정 → 마진 숫자 실시간 갱신 (하드코딩 아님 시연)
    - teardown: 목업 데이터 기반·DB 없음 → 영속 side effect 없음(클라이언트 상태만). 별도 teardown 불필요.

## Verification

- `npm run dev` 후 브라우저에서 3개 라우트(`/`, `/project/[code]`, `/people`)가 모두 렌더링된다.
- 화면①에서 저마진 프로젝트가 🔴로, 양호 프로젝트가 🟢로 구분 표시되고 전사 합계 행이 나온다.
- 화면②에서 배정 M/M(또는 투입률)을 조정하면 마진·마진율 숫자가 즉시 갱신된다(하드코딩이 아님을 시연).
- `npm run test`(vitest)에서 `pnl.ts` 단위 테스트가 통과한다 — 최소: 단일 배정 원가 = 등급원가×M/M, 프로젝트 마진 = 수주−배정원가합, 마진율 = 마진/수주.
- `npm run build`(adapter-static)가 에러 없이 정적 산출물을 생성한다 → 백엔드/DB 없이 배포 가능함이 증명된다.
- 목업 데이터의 마진 숫자가 손으로 검산한 값과 일치한다(예: 수주 4.8억 − 배정원가 2.61억 = 마진 2.19억, 45.6%).

## TC (Test Case)

> `pnl.ts` 순수 함수 중심 단위테스트(vitest). Right-BICEP·CORRECT 기준. 실행·통과 확인은 Node 정적 게이트(Z 섹션)에서 수행한다.

### 단위 테스트 (`src/lib/pnl.test.ts`, vitest)

**Right-BICEP**
- [ ] **Right**: `assignmentCost` = 등급 표준원가 × M/M 정확값 / `projectPnl` 마진 = 수주 − 배정원가합 / `portfolioRollup` 전사 합계·마진율 정확값
- [ ] **Boundary**: M/M = 0 → 원가 0 / 배정 0건 프로젝트 → 배정원가합 0·마진 = 수주 전액 / 적자 프로젝트(원가 > 수주) → 마진 음수·마진율 음수 허용
- [ ] **Inverse**: 마진 = 수주 − 원가합 ↔ 원가합 = 수주 − 마진 역산 일치 확인
- [ ] **Cross-check**: `portfolioRollup` 마진 합 = 개별 `projectPnl` 마진의 단순 합과 일치
- [ ] **Error**: 존재하지 않는 등급 id를 참조하는 배정 → 명시적 실패 또는 0 처리(무음 NaN 오염 금지) 확인
- Performance: 프로토타입 소규모(프로젝트≤4·직원≤8) → **해당 없음**

**CORRECT**
- [ ] **Conformance**: `format.ts` 금액 축약(억/만원)·퍼센트 출력 형식이 규칙과 일치
- [ ] **Ordering**: 포트폴리오 테이블이 정렬 규칙(예: 마진율 오름차순으로 🔴 상단)을 따르면 순서 검증 — 정렬 미적용이면 "해당 없음" 명시
- [ ] **Range**: 마진율 신호등 경계값 정확 분류 — 정확히 30%·15% 및 그 ±ε에서 🟢/🟡/🔴 임계 판정(경계 포함/미포함 규칙 검증)
- [ ] **Reference**: 목업 fixture로 assignment→grade→employee 참조 조인이 올바르게 해석됨
- [ ] **Existence**: 없는 프로젝트 code / 빈 배정 배열 / null 등급 참조가 안전 처리(예외·크래시 없음)
- [ ] **Cardinality**: 프로젝트 0/1/N, 배정 0/1/N 각각 정상 계산
- Time: 동시성·타임아웃 없음(정적 클라이언트) → **해당 없음**

### e2e 통합 테스트

Z-post 섹션 참조 — `e2e/psa-demo.spec.ts`(Playwright)로 3개 라우트 렌더링·신호등 구분·실시간 재계산 인터랙션을 검증한다. 앱 기동 필요로 push 후 실행.

### DB 통합 테스트

**해당 없음** — DB·백엔드 없이 JSON 목업만 사용하는 정적 앱이므로 DB 통합 테스트 대상이 없다.
