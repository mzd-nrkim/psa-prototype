# DESIGN.md — psa_private (사내 PSA)

- **문서 유형**: 디자인 시스템 계약 (스타일의 source of truth)
- **작성일**: 2026-07-06
- **역할**: 색·타이포·간격·형태 등 **스타일 토큰**을 정의한다. 기능 요구사항은 별도 design-prompt/제품정의가 담당하며, 이 문서는 그 요구사항을 어떤 시각 언어로 그릴지를 규정한다.
- **기반**: `lodestar/DESIGN.md`의 토큰 아키텍처(뉴트럴 + 단일 액센트 + 기능색 신호등 3층)를 차용. 단 (1) 뉴트럴을 마우브(#D0CCD0) → 깨끗한 쿨 슬레이트로 교체해 대비를 올리고, (2) 액센트는 한 단계 또렷한 틸로, (3) 상태색 층은 파이프라인 도메인이 아니라 **PSA 도메인**(프로젝트 lifecycle·health·마진·가동률·주간보고)으로 재정의했다.
- **상태 도메인 근거**: `docs/2026-07-03_PSA-핵심-데이터모델-초안.md` (확인됨: 엔티티 상태 축) / hex 값은 전부 (추정).

---

## 브랜드 톤

- **크리스프한 신뢰감**: 매출·원가·마진을 다루는 업무 도구다. 흐릿함은 곧 불신이다 — 텍스트·경계는 **높은 대비**로 또렷하게.
- **차분한 표면, 선택적 선명**: 넓은 표면은 저채도 뉴트럴로 눈을 편하게 두되, 선명함(채도)은 **신호(상태·주요 CTA)에만** 몰아준다. 하루 종일 보는 툴의 정석 = 저채도 + 고대비 + 선택적 선명.
- **뉴트럴 + 단일 액센트**: 화면 대부분은 슬레이트, 상호작용·강조는 틸 하나로.
- **기능색은 신호등**: 프로젝트 상태·마진·가동을 나타내는 색은 뉴트럴·액센트와 **분리된 고정 의미**를 가진다. 브랜드 색으로 이 신호를 흐리지 않는다.
- **라이트 우선**: 밝은 뉴트럴 배경 기준. 다크 테마는 후속(같은 토큰 반전).

## 색상 토큰

의미 기반 3층 구조. raw hex 나열이 아니라 역할로 고정한다.

### 뉴트럴 (표면·텍스트·경계) — 슬레이트 계열, 마우브 제거

| 토큰 | 값 | 역할 |
|---|---|---|
| `--bg` | `#F8FAFC` | 기본 배경 (쿨 니어화이트, slate-50). (추정) |
| `--surface` | `#FFFFFF` | 카드·패널 등 올라온 표면. (추정) |
| `--surface-muted` | `#F1F5F9` | 옅은 구획·비활성 표면·표 헤더 (slate-100). (추정) |
| `--border` | `#E2E8F0` | 경계·구분선 (slate-200). (추정) |
| `--border-strong` | `#CBD5E1` | 강조 경계·입력 테두리 (slate-300). (추정) |
| `--text` | `#1E293B` | 본문·주요 텍스트 (딥 슬레이트, 대비 ~13:1). (추정) |
| `--text-secondary` | `#64748B` | 보조 텍스트·라벨 (slate-500, 대비 ~4.8:1). (추정) |
| `--ink` | `#0F172A` | 최대 대비 잉크 / 다크 표면 베이스 (slate-900). (추정) |

### 액센트 (상호작용·강조) — 한 단계 또렷한 틸

| 토큰 | 값 | 역할 |
|---|---|---|
| `--accent` | `#0E7490` | 링크·주요 액션·선택·포커스 (크리스프 틸, cyan-700, 대비 ~5.3:1). (추정) |
| `--accent-hover` | `#0C5E74` | 액센트 hover/active. (추정) |
| `--accent-weak` | `#ECFEFF` | 액센트 배경(선택된 행·강조 배경, cyan-50). (추정) |

### 기능 상태색 (PSA 신호등 — 의미 고정)

> 넓게 채우지 않는다. 칩·도트·작은 채움·좌측 보더·테두리로 절제해서 쓴다. 팔레트(뉴트럴·액센트) 밖의 별도 색이며, 액센트(틸)와 한 요소에 겹쳐 쓰지 않는다.

**① 프로젝트 health / 마진 (핵심 신호등 — 이 툴의 심장)**

| 토큰 | 값 | 의미 |
|---|---|---|
| `--health-ok` | `#16A34A` | 정상 — 마진 정상·일정 정상·흑자 (green-600). (추정) |
| `--health-watch` | `#D97706` | 주의 — 저마진·경미한 진척 지연 (amber-600). (추정) |
| `--health-risk` | `#DC2626` | 위험 — 적자·심각한 지연·중단 위협 (red-600). (추정) |

**② 프로젝트 lifecycle (제안/진행/완료/중단 — 상태값)**

| 상태 | 표현 | 근거 |
|---|---|---|
| 제안 | `--text-secondary` 뉴트럴 칩 (아직 신호 아님) | 데이터모델 §1 |
| 진행 | `--accent` 틸 칩·도트 (활성) | 데이터모델 §1 |
| 완료 | `--surface-muted` 뮤트 칩 (가라앉힘) | 데이터모델 §1 |
| 중단 | `--health-risk` **점선 테두리** (채움 아님) | 데이터모델 §1 |

**③ 가동률 (billable / 벤치)**

| 토큰 | 값 | 의미 |
|---|---|---|
| `--util-billable` | `#16A34A` | 청구 가능·프로젝트 배정됨 (health-ok와 동일 계열). (추정) |
| `--util-bench` | `#94A3B8` | 벤치·미배정 — 비용이나 위험은 아님, **회색으로 절제** (slate-400). (추정) |

**④ 데이터 상태 (주간보고·누락)**

| 토큰 | 값 | 의미 |
|---|---|---|
| `--status-missing` | `#CBD5E1` | 미작성·조합 없음 — **회색 점선** (slate-300). (추정) |

- **정체·지연 강조**는 채움이 아니라 테두리/도트로 절제해 "미완"임을 드러낸다.
- **위험 전파**(예: 한 프로젝트 적자가 전사 손익에 영향)는 `--health-risk` 실선→점선으로 번지게 하되 굵기는 위험도에 비례.
- **민감 지표**(인당 수익·인건원가)는 색으로 강조하지 않는다 — 권한·텍스트로만. (데이터모델 §2·§3, 정치적 민감)

## 타이포그래피

- **글꼴**: `Inter`(본문·UI), 없으면 시스템 산세리프 폴백(`-apple-system, "Segoe UI", …`). 숫자 표(금액·마진율·M/M)는 `font-variant-numeric: tabular-nums` 강제. 코드/ID 프레임은 `"JetBrains Mono", ui-monospace, monospace`. (추정)
- **밀도 우선 스케일** (compact — 표·대시보드 중심):

| 토큰 | 크기 / 행간 | 용도 |
|---|---|---|
| `--text-xs` | 12 / 16 | 메타·캡션·근거 |
| `--text-sm` | 13 / 18 | 표·칩·보조 라벨 |
| `--text-base` | 14 / 20 | 본문 기본 |
| `--text-lg` | 16 / 24 | 섹션 제목 |
| `--text-xl` | 20 / 28 | 화면 제목·헤드라인 |

- 굵기: 본문 400, 강조 500, 제목 600. 700은 지양(차분함 유지). 금액·마진율 등 핵심 수치는 500으로 또렷하게.

## spacing / radius / elevation

- **간격 기준**: 4px 스케일 — `4 / 8 / 12 / 16 / 24 / 32`. 밀도가 높으므로 8·12를 주력으로. (추정)
- **모서리(radius)**: `--radius-sm 4px`(칩·버튼), `--radius-md 6px`(카드·패널). 크게 둥글리지 않는다(크리스프·전문성). (추정)
- **elevation(그림자)**: 최소. 밝은 표면에서 옅은 `0 1px 2px rgba(15,23,42,.06)` 수준. 경계는 그림자보다 1px `--border`를 우선. (추정)
- **밀도**: 표·목록은 compact 행 높이(28~32px). 여백으로 정보량을 줄이지 않는다.

## 컴포넌트 원칙

- **버튼**: `primary`(액센트 채움) / `secondary`(border-strong 테두리) / `ghost`(무배경). 크기 `sm/md`. 파괴적 액션만 `--health-risk` 테두리.
- **카드/패널**: `--surface` 배경, 1px `--border`, `--radius-md`. 그림자 최소.
- **표(대시보드 핵심)**: `--surface-muted` 헤더, 행 hover `--accent-weak`, 선택 행 좌측 2px `--accent` 보더. 숫자 열 우측 정렬 + tabular-nums.
- **상태 칩**: 텍스트 + 도트. lifecycle·health를 위 상태색 표대로 매핑. 채움은 옅게, 글자는 어둡게.
- **마진율 셀**: 흑자 `--health-ok` / 저마진 `--health-watch` / 적자 `--health-risk`. 배경 채움이 아니라 텍스트 색 + 작은 도트로.
- 일관성: 같은 의미는 항상 같은 토큰. 토큰 외 색상 직접 사용 금지.

## 상태 표현

- **interaction**: hover = 액센트 10% 어둡게 or `--accent-weak` 배경. focus = 2px `--accent` ring(focus-visible 항상). active = `--accent-hover`. disabled = opacity 0.5 + 커서 not-allowed.
- **데이터 상태**: empty = 뉴트럴 안내 메시지 + 아이콘(상태색 금지). loading = skeleton(뉴트럴). error = `--health-risk` 텍스트 + 아이콘. 권한 없음 = 뉴트럴 톤 + 잠금 아이콘(상태색 금지).
- **확신도·근거**는 색이 아니라 텍스트/보조 기호로 — 신호등 색을 잠식하지 않게.
- 액센트(틸)와 상태색을 한 요소에 겹쳐 쓰지 않는다(선택 상태 ≠ 위험 상태 혼동 방지).

## 접근성 / 반응형

- **대비**: 본문 4.5:1(WCAG AA) 이상. `--health-watch`(#D97706)는 소형 텍스트 대비가 낮으므로(~3.4:1), 텍스트로 쓸 땐 `#B45309`로 어둡게 하거나 채움+어두운 글자 조합으로 사용. (추정)
- **색 단독 의존 금지**: 상태는 색 + 아이콘/텍스트/도트 형태를 병기(색각 이상 대응).
- **focus-visible** 항상 표시. 터치/클릭 타깃 최소 32px(compact) — 모바일 확장 시 44px.
- **breakpoint**: 데스크톱 우선(대시보드·표 중심). mobile<768px는 요약 카드로 폴백, desktop≥1024px 풀 테이블. (추정)

## 금지 패턴

- 토큰 외 색상 직접 사용 금지. 임의 그라데이션 금지.
- 뉴트럴에 마우브/웜 그레이 재도입 금지(대비 저하의 원인 — 쿨 슬레이트 유지).
- 상태색을 넓은 표면에 채우기 금지(칩·도트·보더·작은 채움까지만).
- 액센트와 상태색 한 요소 중첩 금지.
- 민감 지표(인당 수익·인건원가·급여)를 색으로 강조 금지 — 권한·텍스트로만.
- 700 웨이트 남용 금지(차분함 훼손).

## 적용 범위

사내 PSA(psa_private) 프로젝트 전체 UI. 현재 코드 없음 — 본 문서는 **와이어프레임·구현에 선행하는 계약**이며, 이후 design-prompt·UI 구현 checklist가 위 토큰/컴포넌트 항목명을 고정 참조한다. 코드 착수 시 실제 값이 확정되면 각 (추정)을 (확인됨)으로 승격한다.
